# Bundle setup

Install or copy the entire redMage plugin folder, including skills, references and scripts. Run `python3 scripts/dependencies.py` from the plugin root. No external skill packages are required. Missing files indicate an incomplete installation: restore them from the same trusted redMage revision and rerun validation. There is no automatic npx lifecycle hook. The root SKILL.md remains a /rdm compatibility entry for full-folder skill installers.
